# Request 复制与 Send Again 设计说明

本文档整理了 `logViewer` 针对请求复现、复制到 Bruno、右键菜单与快捷键的推荐交互。

## 目标

当用户在 `logViewer` 中看到一条网络请求时，应该能快速完成以下动作：

1. 复制 URL / Query / Headers / Body
2. 复制 cURL
3. 重新发送请求
4. 尽量少切换视图、少移动鼠标

## 总体设计

最适合的形态不是继续堆叠多个孤立按钮，而是建立一个统一的 **Request 复制 / 导出体系**：

- Inspector 顶部放统一的 **Copy** 入口
- `Send Again` 保留在旁边
- 各区块支持就地右键复制
- Network 列表行支持快捷右键
- 高频动作再补快捷键

## 入口分层

| 位置 | 文件 | 适合的操作 |
| --- | --- | --- |
| Inspector 顶部 | `LogDetailView.swift` | 完整的 Copy 菜单、Send Again |
| Inspector 各 section | `LogDetailView.swift` | 就地复制当前块 |
| Network 列表行右键 | `PulseConsoleFormatting.swift` 中的 `PulseNetworkRowView` | 快速复制 URL / cURL / Send Again |

## Inspector 顶部动作

建议在 `requestActions(for:)` 中提供：

- `Copy`
- `Send Again`

其中 `Copy` 菜单按 Bruno 常见填写顺序组织：

1. Copy URL
2. Copy Query Parameters
3. Copy Query Parameters as JSON
4. Copy Headers
5. Copy Headers as JSON
6. Copy Body
7. Copy Body as Pretty JSON
8. Copy cURL
9. Copy Full HTTP Request Summary

这种结构比把每一项都做成单独按钮更紧凑，也更符合请求详情页的操作模型。

## Inspector 各 section 右键

### Request Overview

- Copy Full URL
- Copy Host
- Copy Path
- Copy Query String
- Copy Query Parameters as JSON

### Request Headers

- Copy Headers
- Copy Headers as JSON
- Copy Header Names
- Copy This Header

### Request Body

- Copy Body
- Pretty Copy JSON
- Copy as Raw Text
- Save Body As...

### cURL

- Copy cURL
- Send Again

## Network 列表行右键

当用户还没进入 Inspector，只是想快速拿这条请求去 Bruno 里复现时，列表行右键应该提供：

- Copy URL
- Copy cURL
- Send Again
- Copy Headers
- Copy Query Parameters
- Add to Blacklist
- Delete Request

## 快捷键建议

快捷键只给最高频动作即可：

- `⌘⇧U` — Copy URL
- `⌘⇧P` — Copy Query Parameters
- `⌘⇧H` — Copy Headers
- `⌘⇧B` — Copy Body
- `⌘⇧C` — Copy cURL
- `⌘⇧R` — Send Again

推荐通过 `logViewerApp.swift` 中的 `.commands` 实现成全局 Request 菜单，并对“当前选中的 network request”生效。

## 建议的复制格式

为了更适合粘贴到 Bruno，不建议只复制大段难拆的原始文本。

### URL

- 直接复制完整 URL

### Query Parameters

默认文本格式：

```text
page=1
sort=desc
keyword=apple
```

JSON 格式：

```json
{
 "page": "1",
 "sort": "desc",
 "keyword": "apple"
}
```

### Headers

默认文本格式：

```text
Authorization: Bearer xxx
Content-Type: application/json
X-Trace-Id: abc
```

JSON 格式：

```json
{
 "Authorization": "Bearer xxx",
 "Content-Type": "application/json",
 "X-Trace-Id": "abc"
}
```

### Body

- 默认复制原始 body
- JSON 时额外提供 pretty 版本

## 建议的代码落点

1. `PulseConsoleEntityFormatting.swift`
  - 增加可复制的派生文本
2. `LogDetailView.swift`
  - 增加 Copy 菜单与各 section 右键
3. `PulseConsoleFormatting.swift`
  - 为 Network 行右键补复制动作与 Send Again
4. `logViewerApp.swift`
  - 增加全局 commands 与快捷键

## 实施优先级

1. Inspector 顶部 Copy 菜单 + Send Again
2. Network 列表行右键
3. 全局 commands + 快捷键
4. Bruno 专项导出 / Open in Bruno
